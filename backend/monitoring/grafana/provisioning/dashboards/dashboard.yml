# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'YallaCatch Dashboards'
    orgId: 1
    folder: 'YallaCatch'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
